# 激光吸收光谱（IR-LAS）工作原理与工作流程（简要总结）

## 一、工作原理（几句话）

- 分子能级量子化（转动/振动/电子），激光光子能量等于能级差时发生共振吸收：ΔE = hν；
- 吸收强弱由 Beer–Lambert 定律定量描述：α(ν) = S(T)·P·χ·φ(ν)·L（S 线强、P 压力、χ 摩尔分数、φ 线型、L 光程）；
- 关键推论：积分吸光度 ∫α dν = S·P·χ·L **与线型无关** → 测浓度 χ；
  两条谱线的吸收比只依赖温度 → **双线测温**；线宽反映压力；
- 输出：温度、组分浓度、压力、流速；优点是非接触、原位、物种特异、快而定量。

## 二、工作流程（查库 → 建谱 → 反演）

1. 明确测量需求：测什么（T/χ/P）、温度压力范围、时间分辨率；
2. 数据库（HITRAN/HITEMP）里**选谱线**：避开干扰、按 E″ 选对温度敏感的线；
3. 提取谱线参数：ν0、S(296 K)、E″、γ_air、γ_self（半宽 HWHM）、n、δ；
4. **线强温度标定**：S(T) = S(T0)·[Q(T0)/Q(T)]·exp[−(hcE″/k)(1/T−1/T0)]·(受激发射项)；
5. **单位换算**：S[cm⁻²/atm] = S̃ × 7.34×10²¹/T；
6. **算展宽**：多普勒宽 Δν_D = 7.16×10⁻⁷·ν0·√(T/M)；碰撞宽 Δν_C = P·ΣX_k·2γ_k(T)，γ∝(T0/T)^n；二者卷积成 Voigt 线型（Humlíček 数值算法）；
7. **逐线叠加合成理论光谱**：α(ν) = Σⱼ Sⱼ(T)·P·χ·φⱼ(ν)·L；
8. **反演测量**：直接吸收（积分吸收→χ；吸收比→T）/ 谱拟合（最小二乘同时得 T、P、χ）；
9. 落地为传感器（激光器+光纤+探测器），用于激波管、火焰、燃机、内燃机、超燃、爆震等场景。

**一句话概括**：查库取参数 → 线强温度标定 → 算展宽 → Voigt 线型 → 逐线叠加成谱 → 与实测比对反演 T / χ / P / U。

*本仓库的代码即按此流程实现了第 4–7 步（复现教材附录 C/D 算例）。*
