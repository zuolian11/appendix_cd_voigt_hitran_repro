2# 《Spectroscopy and Optical Diagnostics for Gases》附录 C & D 复现与讲解

> 出处：R. K. Hanson, R. M. Spearrin, C. S. Goldenstein, *Spectroscopy and Optical Diagnostics for Gases*, Springer 2016（教材 PDF 附录 C：PAGE 278–279；附录 D：PAGE 280–286）。
> 复现代码位于本目录：`voigt_humlicek.py`（附录 C）、`reproduce_appendixD.py`（附录 D），运行 `python reproduce_appendixD.py` 可一次性输出全部数值 + CSV + SVG 光谱图。
> 目标：① 用代码把书里的"光谱模拟算例"原样复现；② 讲透每一步背后的原理；③ 给出学会它所需的知识与技能清单。

---

## 0. 两个附录在全书中的角色

- **附录 C（Voigt 拟合程序）**：给出一个**数值工具**——计算 Voigt 线型函数的 Matlab 程序（Humlíček 1982 算法的经典实现）。全书（尤其第 8 章"谱线线型"、第 14 章"案例"）所有"需要 Voigt 值"的地方都调用它，附录 D 的算例也明确说"用附录 C 的数值近似"。
- **附录 D（HITRAN 数据库 + 算例）**：给出**数据库用法**——HITRAN 逐线参数如何读（Table D.1/D.2）、参考温度 296 K 下线强的温度标定（式 D.1/D.2/D.6）、单位换算（式 D.3–D.5），并以一个经典算例收尾：**H₂O 7185.6 cm⁻¹（≈1392.67 nm）双线的吸光度谱计算**（T=1000 K、P=1 atm、10% H₂O/空气、L=10 cm）。
- 两者合起来就是一条完整的"光谱模拟流水线"：**查库取参数 → 标定线强 → 算展宽 → 算 Voigt 线型 → 逐线叠加得吸光度谱**。这正是 TDLAS（可调谐二极管激光吸收光谱）传感器设计的核心数值技能。

---

## 1. 附录 C：Voigt 线型与 Humlíček 数值算法

### 1.1 物理：什么是 Voigt 线型

谱线有两条主要展宽通道（教材第 8 章）：

1. **Doppler 展宽（高斯，非均匀）**：分子热运动带来的多普勒频移，Gaussian 分布。
2. **碰撞（压力）展宽（洛伦兹，均匀）**：碰撞缩短受激态寿命（测不准原理 → 能量不确定），Lorentzian。

两者同时存在且可视为相互独立时，真实线型是二者的**卷积**：

```
φ_V(ν) = ∫ φ_D(u)·φ_L(ν−u) du          （式 8.29）
```

它没有解析初等函数表达式，只能数值计算 → 这就是附录 C 存在的理由。

### 1.2 无量纲化与 Voigt a 参数

把频率用 Doppler 1/e 半宽 α_D = Δν_D/(2√ln2) 无量纲化：u = (ν−ν₀)/α_D；定义

```
a = γ_L / α_D = √(ln2) · Δν_L/Δν_D      （γ_L、Δν_L 为洛伦兹 HWHM/FWHM）
```

则归一化（∫φdν=1）的 Voigt 为

```
φ_V(ν) = φ_D(0) · V(a,u),      φ_D(0) = 2√(ln2/π)/Δν_D
V(a,u)  = (a/π) ∫ e^(−y²) / [a² + (u−y)²] dy
```

两个重要特例（也是我们的自检基准）：
- 面积：∫V du = √π；
- 线心：**V(a,0) = e^(a²)·erfc(a)**（教材式 8.36）；
- a→0 退化高斯、a→∞ 退化洛伦兹；a≈√ln2·0.059/0.038≈1.27（本书算例 Line 1）时高斯、洛伦兹"势均力敌"。

### 1.3 Humlíček 1982 算法（代码逻辑）

书中 Voigt.m 把复平面分成 4 个区域，每区用一个**有理分式（rational approximation）**逼近复误差函数 w(z)，配合 `T = Y − i·X` 的复变量技巧：

| 区域 | 判据 | 公式性质 |
|---|---|---|
| I | \|X\|+Y ≥ 15 | 远翼渐近式（洛伦兹尾巴） |
| II | 5.5 ≤ \|X\|+Y < 15 | 中翼有理式 |
| III | Y ≥ 0.195\|X\| − 0.176 | 主体区（线心附近）有理式 |
| IV | 其余 | 远离主体、小 a 时：用 `e^(U_re)·cos(U_im) − P_4(U)` 修正 |

代码逐行移植在 `voigt_humlicek.py`，并用三组自检验证移植正确：
1. **面积**：∫Voigt(X,a)dX ≈ √π（大 a 时因翼部截断略小 ~1e-3，符合预期）；
2. **线心**：Voigt(0,a) = e^(a²)erfc(a)，误差 ~1e-6（Humlíček 文档精度量级）；
3. **交叉验证**：与高斯×洛伦兹的直接 FFT 数值卷积逐点比较，相对误差 ~1e-5。

**注意书中的调用约定**（这是初学者最易踩的坑）：
- 函数 `Voigt(X,Y)` 的 X 是**无量纲**坐标（要先做 X = 2√ln2·(ν−ν₀)/Δν_D），Y 是 a 参数；
- 它返回的是"面积=√π"的版本，不是归一化线型；
- 因此书中给出振幅修正：`k_ν(ν) = amp · Voigt(X,a)`，
  `amp = 2√(ln2)/(√π·Δν_D) · S·P·χ`，这样 ∫k_ν dν = S·P·χ（即积分线强）。
  我们把它封装为 `kappa_voigt(...)` 函数（∫φ=1 的归一化再乘 S·P·χ），供附录 D 直接调用。

### 1.4 为什么用"分区域有理式"而不是直接卷积

- 直接数值卷积是 O(N²)（谱点 × 卷积核点），FFT 也需 O(N log N) 且要小心网格；
- Humlíček 用复误差函数的有理逼近把每条谱线压到 **O(1)** 每点、误差可控（~1e-4~1e-6 峰值），几十条谱线 × 几千个频率点瞬间算完；
- 这正是"把数值细节写进教科书"的意义：工程光谱模拟不需要自己发明算法，但必须**会用、知道近似精度边界**。

---

## 2. 附录 D.1：H₂O 双线吸光度谱算例复现

### 2.1 输入：HITRAN 参数表（Table D.3）怎么读

```
Line    ν0 [cm⁻¹]       S(296K)[cm⁻¹/(molec·cm⁻²)]  γ_air   γ_self   E″[cm⁻¹]  n_air
 1      7185.596571     2.00e-22                     0.0342  0.371    1045.0583  0.62
 2      7185.596909     5.98e-22                     0.0421  0.195    1045.0577  0.62
```

字段解读（对照 Table D.2）：
- **ν0** 线心频率 [cm⁻¹]（1392.67 nm ⇔ 10⁷/1392.67 ≈ 7180 cm⁻¹ 附近；两条线间隔仅 0.00034 cm⁻¹，构成"双线"，靠非常窄的激光线宽才能分辨，这正是 TDLAS 能做的原因）；
- **S(T0)** 每分子线强，参考温度 **T0=296 K**；
- **γ_air / γ_self**：**HWHM（半高半宽）系数**！[cm⁻¹/atm]@296 K——注意 HITRAN 惯例给半宽，两条线的 FWHM 系数是 **2γ**（教材 Table D.2 有醒目注释）；γ_self 比 γ_air 大 3–10 倍 → 说明 H₂O–H₂O 碰撞展宽远强于 H₂O–N₂/O₂，纯水蒸气与潮湿空气的谱宽差异巨大；
- **E″** 低态能量 [cm⁻¹]（两条线几乎相同 1045 cm⁻¹ → 温度标定行为几乎一样）；
- **n_air** 展宽系数的温度指数。

### 2.2 D.1.1 线强温度标定（物理核心）

HITRAN 只在 296 K 给 S。1000 K 的 S 由三个物理因素决定（式 D.2/D.6）：

```
S(T)/S(T0) = [Q(T0)/Q(T)]                      ← ① 配分函数：总布居如何"稀释"
           × exp[−(hc/k)·E″·(1/T − 1/T0)]      ← ② 玻尔兹曼因子：低态 E″ 的布居变化
           × [1−e^(−hcν0/kT)]/[1−e^(−hcν0/kT0)] ← ③ 受激发射（低温端可忽略）
```

**逐项看本例（T0=296→1000 K）**：
- ② 低态 E″=1045 cm⁻¹：hc/k=1.439 cm·K，指数 = −1.439×1045×(1/1000−1/296) ≈ +3.58 → 因子 **e^3.58 ≈ 35.7**（升温让高 E″ 态布居暴涨——1000 K 时热布居把更多分子送到 v″=2 等高位，这正是"高温测量要选高 E″ 谱线"的原因）；
- ① 配分函数：H₂O 为非线性多原子，Q_rot∝T^{3/2} 再乘 3 个谐振子振动因子（基频 3657.05/1594.75/3755.93 cm⁻¹）→ Q(1000)/Q(296) ≈ (1000/296)^{1.5} × 1.12 ≈ **7.0** → 因子 Q(T0)/Q(T) ≈ 1/7 ≈ 0.143（能级变多，每条线分到的布居变少）；
- ③ 受激发射项 ≈ 0.99997 ≈ 1（7185.6 cm⁻¹ ⇔ 10339 K ≫ 1000 K，激发态几乎空）。

综合：S(1000K)/S(296K) ≈ 35.7/7.0 ≈ **5.1** → S₁=2.00e-22×5.1≈**1.02e-21**、S₂=5.98e-22×5.1≈**3.05e-21** cm⁻¹/(molecule·cm⁻²)，与书一致 ✓。

> 实现细节：书给了两种途径——式 D.2（RRHO：把 Q(T0)/Q(T) 解析写成 (T0/T)^{3/2}×Π 振动比，无需查表）与式 D.6（直接用 HITRAN 配分函数表 Q(T)，精度更高）。教材 Fig. D.1 指出 RRHO 在 296–1500 K 与 HITRAN Q 误差 <2%，1500–3000 K 线性恶化到 ~5%。我们的代码默认 RRHO（约 0.5% 误差），若想精确复现书值可换成 HITRAN parsum 的 Q(T) 表。

### 2.3 单位换算：每分子 → 每 atm（式 D.3）

吸收定律里习惯用"每 atm 线强"S[cm⁻²/atm]（k_ν = S·P·χ·φ，P 用 atm）。换算就是乘 1 atm 下的分子数密度：

```
S[cm⁻²/atm] = S̃[cm⁻¹/(molecule·cm⁻²)] × n(1 atm, T)/1 atm = S̃ × 7.34×10²¹/T
T=296 K 时系数 2.4797×10¹⁹（式 D.4）
```

本例 T=1000 K → 系数 7.34×10¹⁸：
- Line 1: 1.02e-21 × 7.34e18 ≈ **7.5×10⁻³ cm⁻²/atm**（书 7.487e-3 ✓）；
- Line 2: 3.05e-21 × 7.34e18 ≈ **2.24×10⁻² cm⁻²/atm**（书中此处打印 2.237×10⁻³——与其自给的 S₂(1000K)=3.05×10⁻²¹ 及同一换算因子**自相矛盾一个数量级**，疑为排版/抽取笔误，物理上应取 ≈2.24×10⁻²；按 2.24×10⁻² 算出的总峰吸光度 ~0.29 也与书内 Fig. D.3 纵轴（0–0.35、Sum≈0.3）吻合）。

### 2.4 D.1.2 线型：Doppler 宽 + 碰撞宽 → Voigt

**Doppler FWHM**（式 8.25）：

```
Δν_D = 7.1623×10⁻⁷ × ν0 × √(T/M),   M=18.015 g/mol（H₂O）
     = 7.1623×10⁻⁷ × 7185.6 × √(1000/18) ≈ 0.0384 cm⁻¹
```

两条线 ν0 几乎相同 → Δν_D 相同（书 0.0384 ✓）。注意 Δν_D ∝ ν0：同温度下高频线更宽。

**碰撞 FWHM**（式 8.19 + 8.21）——**混合气体逐组分加权**：

```
Δν_C = P · Σ_k X_k · 2γ_k(T)
γ_k(T) = γ_k(296 K)·(296/T)^n
```

本例 X_H₂O=0.1、X_air=0.9：
- Line 1: 2×[0.9·γ_air(1000) + 0.1·γ_self(1000)]
  γ_air(1000)=0.0342·(0.296)^0.62≈0.0161；γ_self(1000)=0.371·(0.296)^0.75≈0.149（自加宽温度指数取 **0.75**，书内假设）
  → Δν_C ≈ 2×(0.9×0.0161+0.1×0.149) = **0.0587 cm⁻¹** ✓
- Line 2: 同理 0.0421→0.0198、0.195→0.0782 → Δν_C = **0.0513 cm⁻¹** ✓

**Voigt a 参数**：a = √ln2·Δν_C/Δν_D ≈ 1.27（Line 1）、1.11（Line 2）——碰撞宽 > Doppler 宽，谱线以洛伦兹特征为主，但高斯贡献仍不可忽略，必须用 Voigt 而非纯洛伦兹。

### 2.5 D.1.3 吸光度谱（最终合成）

单线吸光度与总吸光度：

```
α_j(ν) = S_j(T)[cm⁻²/atm] × P[atm] × X_H₂O × φ_j(ν)[cm] × L[cm]
α(ν)   = Σ_j α_j(ν)          （逐线相加是"可加近似"的基石：线间无干涉）
T(ν)   = exp[−α(ν)]          （Beer–Lambert）
```

**我们复现的关键结果**（网格 7185.2–7186.0 cm⁻¹，步长 0.001）：
- Line 1 峰 α ≈ 0.067；Line 2 峰 α ≈ 0.220（S 大 3 倍 → 主导吸收）；
- **总峰 α ≈ 0.287，最小透射率 ≈ 0.75**（≈75% 光被吸收，落在"最优吸光度 0.1–3"区间的偏弱端，若做传感会希望 α~1 更佳——可通过加长光程/提高 X 实现）；
- 窗口积分吸光度 ∫α dν ≈ 0.0288 cm⁻¹ ≈ S₁·P·X·L + S₂·P·X·L = 0.0300（尾翼截断导致略小）——再次验证"积分吸光度与线型无关、只等于线强×P×χ×L"这一全书反复强调的结论。

---

## 3. 复现结果 vs 书中数值对照表

| 量 | 我们的结果 | 书中数值 | 备注 |
|---|---|---|---|
| S₁(1000 K) [cm⁻¹/(molec·cm⁻²)] | 1.025e-21 | 1.02e-21 | RRHO 近似 ~0.5% |
| S₂(1000 K) | 3.066e-21 | 3.05e-21 | 同上 |
| Δν_D (两条线) | 0.0383 | 0.0384 | M 取 18.015 的差异 |
| Δν_C Line 1 | 0.0587 | 0.0587 | 精确吻合 |
| Δν_C Line 2 | 0.0513 | 0.0513 | 精确吻合 |
| S₁ → cm⁻²/atm | 7.53e-3 | 7.487e-3 | RRHO 差异 |
| S₂ → cm⁻²/atm | 2.25e-2 | (印)2.237e-3 | 见 2.3 节注：应为 ~2.24e-2 |
| 总峰吸光度 | 0.287 | Fig. D.3 ≈0.3 | 与图吻合 |

结论：**算法与书完全一致；数值差异仅来自"RRHO vs HITRAN 配分函数"（<2%）和一个疑似排版笔误**。代码注释里都标了出处。

---

## 4. 你需要掌握的知识（理论层）

1. **分子光谱学基础**（对应教材第 1–2 章）：量子化能级、ΔE=hν、转动/振动能级、配分函数 Q=Q_rot·Q_vib·Q_elec、Boltzmann 布居、LTE；为什么 E″ 决定谱线的温度敏感性。
2. **定量吸收**（第 7 章）：Beer–Lambert；吸收系数 k_ν=S·φ·P·χ；积分吸光度 A=∫α dν=S·P·χ·L 与线型无关；线强温度标定式中三个因子的物理来源（配分函数、玻尔兹曼、受激发射）；Einstein A/B 与振子强度概念（HITRAN 里 A 字段与之相关）。
3. **线型物理**（第 8 章）：均匀 vs 非均匀展宽；Doppler（高斯，∝√(T/M)）、碰撞（洛伦兹，混合气体加权 + 温度幂律）、Voigt 卷积与 a 参数；线移 δ；认识 Voigt 的局限（Dicke 窄化、速度依赖展宽 → Rautian/Galatry 等，第 14 章 H₂O 案例会用到）。
4. **单位换算的"单位卫生"**：cm⁻¹/(molecule·cm⁻²) vs cm⁻²/atm vs cm⁻¹/atm(HWHM vs FWHM 2γ)；参考温度 296 K；温度标定必须用同一套单位（式 D.5）。**这是光谱数值工作里最常见的错误来源**。

## 5. 你需要掌握的技能（操作/计算层）

1. **读懂 HITRAN/HITEMP 参数**：hitran.org 下载 .par 逐线文件（19 字段）、或 API/网页版查询；理解同位素体码、全局/局域量子数 v″v′、J″ 等；HITEMP 覆盖高温线。
2. **数值线型计算**：会用/能移植 Humlíček（或 scipy.special.voigt_profile）并**验证**（面积、线心解析值、与卷积对照——本仓库已演示三种验证法）；知道近似算法精度边界。
3. **谱线叠加与向量化**：`α(ν)=Σⱼ Sⱼ(T)Pχφⱼ(ν)L`；用数组运算一次算完所有线（Python/NumPy/MATLAB）；避免逐频点 for 循环。
4. **谱拟合反演（进阶）**：给实测透射谱，用 Voigt + 最小二乘拟合反推 T、P、χ（附录 C 注释"To use with curve-fitting…"即此意；教材附录 B 的 Voigt 数值表 + C 的程序就是为此准备）。进一步：两线测温（R=S(T,ν₁)/S(T,ν₂)→T）。
5. **数据与可视化**：本仓库输出 CSV（便于 Excel/后续处理）与 SVG 谱图；实际工作中用 matplotlib 等直接看谱、看残差。

## 6. 建议的自学练习（按难度递进）

1. 把 T 改成 296 K 和 3000 K 重跑脚本：观察 S、Δν_D、Δν_C、α 峰怎么变（S 随 T 先降后升于 E″~kT 处反转；Δν_D∝√T；γ∝T^(−n)）。
2. 把 X_H₂O 从 0.1 改到 0.01 和 1.0：体会"自加宽项权重变化"对 Δν_C 的影响。
3. 只保留一条谱线 vs 两条：量化"双线重叠误差"（本例双线间距 0.00034 cm⁻¹ ≪ 线宽，单激光几乎无法分辨，实际 TDLAS 常按"两线带"处理或选更分开的线对）。
4. 自己从 HITRAN 下载 CO 或 CH₄ 的几条线，写一个 `simulate(nu, T, P, X, L)` 泛化函数，对比 SpectraPlot/hitran.org 网页模拟结果。
5. 进阶：给"实测"谱（脚本生成后加噪声）做 Voigt 谱拟合，反推 T 与 χ，估算不确定度（对应教材 14.3 的"线型参数与模型强耦合"警告：**用 Voigt 拟合得到的 γ 与模型选择有关**）。

---

## 7. 本目录文件清单

| 文件 | 说明 |
|---|---|
| `voigt_humlicek.py` | 附录 C 的 Python 移植（NumPy 向量化）+ 3 组自检（面积/线心/与卷积对照） |
| `reproduce_appendixD.py` | 附录 D.1 完整算例：线强标定→单位换算→Doppler/碰撞宽→Voigt→α谱，输出对照表 |
| `h2o_doublet_absorbance.csv` | 频率网格上的 α₁、α₂、α_total、透射率 |
| `h2o_doublet_spectrum.svg` | 谱图（浏览器可直接打开；因环境无 matplotlib 用手写 SVG 生成） |
| 本文件 | 原理讲解 + 知识/技能清单 + 练习建议 |
